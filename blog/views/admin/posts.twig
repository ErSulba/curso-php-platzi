{% extends "layout.twig" %}

{% block content %}

    <h2> Posts </h2>
    <a class="btn btn-primary" href="{{ 'posts/create' }}"> new post</a>
    <table class="table">
        <thead>
        <tr>
            <th>Title</th>
            <th> Edit</th>
            <th> Delete</th>
        </tr>
        </thead>
        {% for blogPost in blogPosts %}
            <tr>
                <td> {{  blogPost.title }} </td>
                <td> Edit</td>
                <td> Delete</td>
            </tr>
        {% endfor %}

    </table>
{% endblock %}
